#
# --- TREKKING WEB APP DEVELOPMENT ENVIRONMENT VARIABLES ---
#
# This file serves as a template for the .env file.
# Copy this file to .env and uncomment/change values if needed.
# The default values used here are implemented in the docker-compose.yaml file.

# =================================================================
# 1. NETWORK AND PORTS (Convention over Configuration)
# =================================================================

# The internal/external port for the Express Server (Default: 3000)
# Express listens internally on this port, and Docker exposes it externally.
# SERVER_PORT=3000

# The hostname of the server service within the Docker network.
# Used by Vite's proxy configuration (e.g., http://server:3000).
# SERVER_HOST=server

# The internal/external port for the Vue Client/Vite Dev Server (Default: 5173)
# CLIENT_PORT=5173


# =================================================================
# 2. APPLICATION CONFIGURATION
# =================================================================

# Flag to determine if the Server should attempt to connect to ATLAS or use Local MongoDB.
# Set to 'true' to attempt connection to the external URI in secrets/mongo_atlas_uri.txt
# USE_ATLAS=true


# =================================================================
# 3. SECRETS REMINDER (DO NOT PUT SECRETS HERE!)
# =================================================================

# The MongoDB Atlas connection string must be placed in a separate, untracked file.
# Action: Create the file 'secrets/mongo_atlas_uri.txt' and paste the full URI inside.